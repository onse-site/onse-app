{
  "name": "onse-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "app": "concurrently \"(rm -rf ./server/public/* && cd client && npm run build && cp -r dist/* ../server/public/)\" \"(cd server && npm run server)\"",
    "dev": "concurrently \"(cd client && npm run build && cp -r dist/* ../server/public/)\" \"(cd server && npm run server)\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
